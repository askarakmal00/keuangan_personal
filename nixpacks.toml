[phases.setup]
nixPkgs = ['nodejs_20', 'python311', 'gcc', 'gnumake', 'pkg-config', 'openssl']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run start'
